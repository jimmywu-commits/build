/* ════════════════════════════════════════════════════════
   render-config.js
   ────────────────────────────────────────────────────────
   這份檔案放的是「我 (Claude) 針對你的 PS CSS 額外做的調整」的可調參數，
   跟 72 份 block.json（你的原始座標資料）分開放。

   要調整數值，直接改下面這些數字，存檔後重新整理網頁就會生效，
   不用重新產生積木、不用碰其他程式碼。

   注意：這個檔案本質上是 JavaScript，不是純資料檔。改的時候要小心：
   - 每一行結尾的逗號不要漏掉、也不要多加（最後一個屬性後面不要有逗號）
   - 大括號 { } 要成對，改完之後檢查一下有沒有不小心刪到或多打
   - 文字類的值要包在引號裡（例如 "15px"），數字不用
   如果改完之後畫面整個空白或報錯，通常就是這類語法小錯，
   回頭檢查逗號、括號有沒有對齊，或是把改動還原回來對照看看。
════════════════════════════════════════════════════════ */

window.BN_RENDER_CONFIG = {

  // ──────────────────────────────────────────────
  // 文字垂直位置微調
  // ──────────────────────────────────────────────
  // PS 匯出的文字 top，跟瀏覽器實際畫出文字的位置，系統性地差了
  // 「幾乎正好一個字體大小」（用你的參考圖實測比對出來的規律）。
  // 所以每個文字圖層預設都會自動用「top − 字體大小」抓實際顯示位置，
  // 不需要額外設定。
  //
  // 下面這幾個數字，是「在那個自動計算的基礎上，再往下(正數)或往上(負數)
  // 微調幾 px」，預設都是 0，代表完全照自動計算的結果、不額外調整。
  //
  // 舉例：如果覺得促標文字實際看起來還要再往下移 5px，
  //       把 promo 改成 5 即可；要往上移，改成 -5。
  textVerticalCorrection: {
    promo: -1,
    name: -3,
    warn: 3,
    badgeText: 0,
    ctaText: -3,
    logoText: 30
  },

  // ──────────────────────────────────────────────
  // 文字字距（每個字左右的間距）
  // ──────────────────────────────────────────────
  // 單位是 px，數字是「疊加在字與字之間的額外間距」：
  //   0 = 原本字體預設的字距，不調整
  //   負數 = 字跟字擠緊一點（例如 -20 會讓字距明顯變窄）
  //   正數 = 字跟字拉開一點
  letterSpacing: {
    promo: -1.5,
    name: 0,
    warn: 0,
    badgeText: 0,
    ctaText: 0,
    logoText: 0
  },

  // ──────────────────────────────────────────────
  // 圓標文字
  // ──────────────────────────────────────────────
  badge: {
    maxWidth: 120,     // 文字超過這個寬度(px)就會自動斷行
    lineHeight: 40    // 斷成兩行時，每行之間的距離(px)
    // 改這兩個數字，置中位置會自動重新計算，不用擔心跑版
  },

  // ──────────────────────────────────────────────
  // LOGO圖 / 商品圖 的縮放方式
  // ──────────────────────────────────────────────
  // 數字是「圖片相對於框的大小百分比」：
  //   100 = 完整填滿框、依寬或高哪邊先頂到邊自動縮小，不裁切
  //    70 = 只佔框的70%大小，四周留白置中
  image: {
    logoInsetScalePercent: 90,
    productImageInsetScalePercent: 100
  },

  // ──────────────────────────────────────────────
  // 卡片圓角
  // ──────────────────────────────────────────────
  cardStyle: {
    cardCornerRadius: 15,          // 卡片背景本身的圓角
    promoBarTopCornerRadius: 15    // 促標底(頂部橫幅)頂部兩個角的圓角
    // 這兩個通常要設一樣，畫面才會是無縫的圓角
  }

};
